name: Update Configs

on:
  schedule:
    - cron: "0 */2 * * *"
  workflow_dispatch:

concurrency:
  group: update-configs
  queue: single
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Load sources
        env:
          PROXY_SOURCES: ${{ secrets.PROXY_SOURCES }}
        run: |
          test -n "$PROXY_SOURCES"
          printf '%s\n' "$PROXY_SOURCES" > sources.txt

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: stable
          cache: false

      - name: Cache Go build and module data
        uses: actions/cache@v4
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
          key: ${{ runner.os }}-go-singbox-v1.14.2
          restore-keys: |
            ${{ runner.os }}-go-singbox-

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies and build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-rust-${{ hashFiles('Cargo.toml') }}
          restore-keys: |
            ${{ runner.os }}-rust-

      - name: Cache Python packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-singbox2proxy
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Cache sing-box binary
        id: sing-box-cache
        uses: actions/cache@v4
        with:
          path: ~/.local/bin/sing-box
          key: ${{ runner.os }}-sing-box-v1.14.2-with_quic-with_wireguard-with_utls-with_clash_api-with_gvisor

      - name: Install proxy tester
        run: |
          python3 -m pip install --disable-pip-version-check 'singbox2proxy[socks]'
          mkdir -p "$HOME/.local/bin"
          if [ ! -x "$HOME/.local/bin/sing-box" ]; then
            git clone --depth 1 --branch v1.14.2 https://github.com/SagerNet/sing-box.git /tmp/sing-box
            cd /tmp/sing-box
            TAGS="with_quic with_wireguard with_utls with_clash_api with_gvisor" make
            install -m 0755 sing-box "$HOME/.local/bin/sing-box"
          fi
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          "$HOME/.local/bin/sing-box" version
          "$HOME/.local/bin/sing-box" version | grep -q 'with_utls' || {
            echo "sing-box was built without with_utls"
            exit 1
          }

      - name: Check runner network
        run: |
          curl -fsS --max-time 10 https://cp.cloudflare.com/ -o /dev/null
          curl -fsS --max-time 10 https://api.ipify.org?format=json -o /dev/null

      - name: Preserve previous Light pool
        run: |
          mkdir -p /tmp/proxy-harvester
          if [ -s subscriptions/light.txt ]; then
            cp subscriptions/light.txt /tmp/proxy-harvester/previous-light.txt
          else
            : > /tmp/proxy-harvester/previous-light.txt
          fi

      - name: Build
        run: |
          # Rust performs source collection, normalization and the cheap global
          # TCP reachability pass. Final Light selection is done once, globally,
          # after that pass so weak protocols cannot consume the Light quota.
          python3 - <<'PY'
          from pathlib import Path
          p = Path("src/main.rs")
          s = p.read_text()
          replacements = {
              "const PROXY_TEST_LIMIT_PER_PROTOCOL: usize = 500;": "const PROXY_TEST_LIMIT_PER_PROTOCOL: usize = 3000;",
              "const PROXY_TEST_TIMEOUT_SECS: u64 = 3;": "const PROXY_TEST_TIMEOUT_SECS: u64 = 6;",
              "const PROXY_TEST_WORKERS: usize = 10;": "const PROXY_TEST_WORKERS: usize = 12;",
              "const LIGHT_CANDIDATE_BUDGET: usize = 12000;": "const LIGHT_CANDIDATE_BUDGET: usize = 0;",
              "const GEO_PER_PROTOCOL_LIMIT: usize = 1000;": "const GEO_PER_PROTOCOL_LIMIT: usize = 0;",
          }
          for old, new in replacements.items():
              if old not in s:
                  raise SystemExit(f"expected source constant not found: {old}")
              s = s.replace(old, new, 1)
          p.write_text(s)
          PY
          cargo build --release

      - name: Collect and TCP-test configs
        run: ./target/release/proxy-harvester

      - name: Polish Recommended Light subscription
        run: |
          python3 scripts/polish_light.py \
            --all subscriptions/all.txt \
            --seed /tmp/proxy-harvester/previous-light.txt \
            --output subscriptions/light.txt \
            --checker scripts/check_proxies.py \
            --budget 16000
          python3 - <<'PY'
          from pathlib import Path
          p = Path("subscriptions/light.txt")
          lines = [x.strip() for x in p.read_text().splitlines() if x.strip()]
          if lines:
              p.write_text("\n".join(lines[:200]) + "\n")
          print(f"Recommended Light contains {len(lines[:200])} configs")
          PY

      - name: Commit changes
        run: |
          git config user.name "rekt0ro"
          git config user.email "vultazhh@gmail.com"

          git add subscriptions/ scripts/check_proxies.py scripts/polish_light.py .github/workflows/update.yml

          if git diff --cached --quiet; then
            echo "No changes."
            exit 0
          fi

          git commit -m "Polish Light with global stability-first selection"
          git push
